# API Yatube — REST API для социальной сети

Полнофункциональный REST API для управления постами, комментариями, группами и подписками в социальной сети Yatube. Построен на Django REST Framework с поддержкой JWT-аутентификации и throttling.

## Возможности

- 📝 **Посты**: создание, чтение, редактирование, удаление постов с поддержкой изображений
- 💬 **Комментарии**: добавление комментариев к постам с привязкой к автору
- 👥 **Группы**: группировка постов по категориям (только чтение)
- 🔗 **Подписки**: подписка на других пользователей с поиском по username
- 🔐 **Аутентификация**: JWT-токены для безопасного доступа
- ⏱️ **Throttling**: ограничение частоты запросов по эндпоинтам
- 📄 **Пагинация**: LimitOffsetPagination для постов
- 🔍 **Фильтрация**: фильтрация постов по группе, поиск по подпискам

## Технологический стек

- **Python 3.12**
- **Django 5.1.1** — веб-фреймворк
- **Django REST Framework 3.15.2** — REST API
- **djangorestframework-simplejwt 5.4.0** — JWT-аутентификация
- **djoser 2.3.1** — управление пользователями
- **django-filter 3.x** — фильтрация
- **Pillow 11.0.0** — работа с изображениями
- **pytest-django 4.9.0** — тестирование

## Установка

### Клонирование репозитория

```bash
git clone https://github.com/olegrogovenko/api-final-yatube.git
cd api-final-yatube
```

### Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Применение миграций

```bash
cd yatube_api
python manage.py migrate
```

### Запуск сервера

```bash
python manage.py runserver
```

Сервер будет доступен по адресу: `http://127.0.0.1:8000/`

## Структура проекта

```
yatube_api/
├── api/                    # Приложение API
│   ├── views.py           # ViewSet-ы для всех ресурсов
│   ├── serializers.py     # Сериализаторы для моделей
│   ├── permissions.py     # Пользовательские разрешения
│   └── urls.py            # Маршруты API
├── posts/                  # Приложение постов
│   ├── models.py          # Модели: Post, Comment, Group, Follow
│   └── migrations/        # Миграции БД
├── yatube_api/            # Главное приложение проекта
│   ├── settings.py        # Конфигурация Django
│   ├── urls.py            # Главные маршруты
│   └── wsgi.py            # WSGI-приложение
└── manage.py              # Django-команды
```

## API Эндпоинты

### Аутентификация

- `POST /api/v1/jwt/create/` — получить JWT-токены (username + пароль)
- `POST /api/v1/jwt/refresh/` — обновить access-токен
- `POST /api/v1/jwt/verify/` — проверить токен

### Посты

- `GET /api/v1/posts/` — список всех постов (с пагинацией и фильтрацией по группе)
- `POST /api/v1/posts/` — создать новый пост (требуется аутентификация)
- `GET /api/v1/posts/{id}/` — получить пост
- `PUT /api/v1/posts/{id}/` — обновить пост (только автор)
- `PATCH /api/v1/posts/{id}/` — частичное обновление (только автор)
- `DELETE /api/v1/posts/{id}/` — удалить пост (только автор)

### Комментарии

- `GET /api/v1/posts/{post_id}/comments/` — список комментариев к посту
- `POST /api/v1/posts/{post_id}/comments/` — добавить комментарий
- `GET /api/v1/posts/{post_id}/comments/{id}/` — получить комментарий
- `PUT /api/v1/posts/{post_id}/comments/{id}/` — обновить комментарий
- `PATCH /api/v1/posts/{post_id}/comments/{id}/` — частичное обновление
- `DELETE /api/v1/posts/{post_id}/comments/{id}/` — удалить комментарий

### Группы

- `GET /api/v1/groups/` — список всех групп
- `GET /api/v1/groups/{id}/` — получить информацию о группе

### Подписки

- `GET /api/v1/follows/` — список подписок текущего пользователя (поиск по username)
- `POST /api/v1/follows/` — подписаться на пользователя

## Примеры использования

### Получение JWT-токена

```bash
curl -X POST http://localhost:8000/api/v1/jwt/create/ \
  -H 'Content-Type: application/json' \
  -d '{
    "username": "testuser",
    "password": "testpass123"
  }'
```

Ответ:
```json
{
  "access": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "refresh": "eyJ0eXAiOiJKV1QiLCJhbGc..."
}
```

### Создание поста

```bash
curl -X POST http://localhost:8000/api/v1/posts/ \
  -H 'Authorization: Bearer {access_token}' \
  -H 'Content-Type: application/json' \
  -d '{
    "text": "Мой первый пост!",
    "group": 1
  }'
```

### Получение списка постов с фильтром

```bash
curl http://localhost:8000/api/v1/posts/?group=1&limit=10&offset=0
```

### Подписка на пользователя

```bash
curl -X POST http://localhost:8000/api/v1/follows/ \
  -H 'Authorization: Bearer {access_token}' \
  -H 'Content-Type: application/json' \
  -d '{
    "following": "username"
  }'
```

## Ограничения частоты запросов (Throttling)

- **Посты**: 100 запросов в день
- **Комментарии**: 300 запросов в день
- **Группы**: 1000 запросов в день
- **Подписки**: 200 запросов в день

## Тестирование

Запуск всех тестов:

```bash
pytest
```

Запуск тестов конкретного модуля:

```bash
pytest tests/test_post.py
pytest tests/test_comment.py
pytest tests/test_follow.py
pytest tests/test_group.py
```

Запуск с покрытием кода:

```bash
pytest --cov=api --cov=posts
```

## Разрешения (Permissions)

- **`IsAuthorOrReadOnly`** — автор может редактировать/удалять, остальные только читают
- **`IsAuthenticated`** — требуется аутентификация (для подписок и создания контента)

## Документация API

После запуска сервера документация доступна по адресу:

- **ReDoc**: `http://127.0.0.1:8000/redoc/`

## Переменные окружения

Для продакшена измените следующие значения в `yatube_api/yatube_api/settings.py`:

```python
DEBUG = False
SECRET_KEY = 'your-secret-key-here'
ALLOWED_HOSTS = ['your-domain.com']
```

## Лицензия

MIT License — см. файл [LICENSE](LICENSE)

## Автор

Олег Роговенко

---

**Связь**: Если у вас есть вопросы или предложения, создайте Issue или Pull Request.
